#
# This file is managed by puppet
#


/usr/bin/mysqldump foreman | gzip --best --to-stdout > \
/opt/mysql-backups/$(date +%F_%H:%M).foreman.sql.gz && \
scp /opt/mysql-backups/$(date +%F_%H:%M).foreman.sql.gz puppet.example.com:/opt/mysql-backups
